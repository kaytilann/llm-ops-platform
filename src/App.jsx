import React,{useState}from"react";import{createRoot}from"react-dom/client";import{motion}from"framer-motion";import{Activity,AlertTriangle,BarChart3,Bot,BrainCircuit,CheckCircle2,ChevronRight,ClipboardCheck,DollarSign,FileSearch,Gauge,GitBranch,Home,LockKeyhole,Network,Radar,Route,Scale,ShieldCheck,Sparkles,Timer,Zap}from"lucide-react";import{AreaChart,Area,BarChart,Bar,CartesianGrid,ComposedChart,Legend,Line,LineChart,Pie,PieChart,RadarChart,PolarGrid,PolarAngleAxis,PolarRadiusAxis,Radar as RRadar,ResponsiveContainer,Tooltip,XAxis,YAxis}from"recharts";import{overviewKpis,usageTrend,promptMetrics,tokenBreakdown,evalRuns,evalTable,routingRules,routingImpact,governanceEvents,policyScores,architectureNodes}from"./data/mockData";import"./styles.css";
const views=[{id:"overview",label:"Overview",icon:Home},{id:"prompts",label:"Prompt Analytics",icon:BarChart3},{id:"evals",label:"Evaluation Framework",icon:ClipboardCheck},{id:"routing",label:"AI Routing",icon:Route},{id:"governance",label:"Governance",icon:ShieldCheck},{id:"architecture",label:"Architecture",icon:Network}];
function Badge({children,tone="neutral"}){return <span className={`badge ${tone}`}>{children}</span>}function Card({children,className=""}){return <motion.section className={`card ${className}`} initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:.25}}>{children}</motion.section>}function SectionTitle({icon:Icon,title,subtitle}){return <div className="sectionTitle"><Icon size={20}/><div><h3>{title}</h3><p>{subtitle}</p></div></div>}function Kpi({item}){return <Card className="kpi"><div className="cardTop"><p>{item.label}</p><Badge tone={item.tone}>{item.delta}</Badge></div><h2>{item.value}</h2><span>{item.caption}</span></Card>}
function Insight({icon:Icon,title,value,copy}){return <Card><div className="insightIcon"><Icon size={20}/></div><h3 className="bigValue">{value}</h3><h4>{title}</h4><p>{copy}</p></Card>}
function Overview(){return <><div className="kpiGrid">{overviewKpis.map(k=><Kpi key={k.label} item={k}/>)}</div><div className="grid two"><Card className="wide"><SectionTitle icon={Activity} title="AI Traffic + Cache Efficiency" subtitle="Requests handled by the platform and share served from reusable prompt/context prefixes."/><div className="chart"><ResponsiveContainer width="100%" height={300}><ComposedChart data={usageTrend}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="day"/><YAxis/><Tooltip/><Legend/><Bar dataKey="requests" name="Requests"/><Line dataKey="cached" name="Cache hits" strokeWidth={3}/></ComposedChart></ResponsiveContainer></div></Card><Card><SectionTitle icon={Sparkles} title="Executive AI Brief" subtitle="Auto-generated from cost, latency, quality, and governance telemetry."/><div className="brief"><h4>This week’s platform story</h4><p>Model routing and prompt caching reduced projected monthly spend by <b>$49K</b> while improving P95 latency by <b>38%</b>. Quality increased after routing support and tool-use workflows to the eval-winning model.</p><div className="briefActions"><button>Generate board update</button><button>Export PDF</button></div></div></Card></div><div className="grid three"><Insight icon={DollarSign} title="Cost optimization" value="$68K/mo" copy="Savings from cache reuse, model routing, and prompt compression."/><Insight icon={Gauge} title="Reliability" value="99.93%" copy="Gateway uptime with fallbacks across model providers."/><Insight icon={LockKeyhole} title="Risk control" value="4.2M" copy="Sensitive tokens scanned before reaching model endpoints."/></div></>}
function PromptAnalytics(){const[selected,setSelected]=useState(promptMetrics[0]);return <div className="grid page"><Card className="wide"><SectionTitle icon={BarChart3} title="Prompt Analytics" subtitle="Track cost, latency, cache hit rate, token usage, and risk by AI application."/><div className="table"><div className="row header"><span>Application</span><span>Requests</span><span>Cache Hit</span><span>Cost</span><span>Latency</span><span>Risk</span></div>{promptMetrics.map(row=><button className={`row ${selected.app===row.app?"selected":""}`} onClick={()=>setSelected(row)} key={row.app}><span>{row.app}</span><span>{row.requests.toLocaleString()}</span><span>{row.cacheHit}%</span><span>${row.cost.toLocaleString()}</span><span>{row.latency}s</span><span><Badge tone={row.risk==="High"?"bad":row.risk==="Medium"?"warn":"good"}>{row.risk}</Badge></span></button>)}</div></Card><Card><SectionTitle icon={FileSearch} title={selected.app} subtitle="Selected application telemetry."/><div className="detailStats"><div><p>Monthly cost</p><h3>${selected.cost.toLocaleString()}</h3></div><div><p>Cache hit rate</p><h3>{selected.cacheHit}%</h3></div><div><p>P95 latency</p><h3>{selected.latency}s</h3></div></div><div className="recommendation"><BrainCircuit size={16}/><span>Recommended: normalize system prompt prefixes and reduce retrieved context by 18% to lower spend without hurting quality.</span></div></Card><Card><SectionTitle icon={Bot} title="Token Breakdown" subtitle="Where token spend is going across the selected app."/><div className="chart small"><ResponsiveContainer width="100%" height={250}><PieChart><Pie data={tokenBreakdown} dataKey="value" nameKey="name" outerRadius={88} label/></PieChart></ResponsiveContainer></div></Card><Card className="wide"><SectionTitle icon={Timer} title="Latency + Cost Trend" subtitle="Identify workflows where user experience and model spend are drifting."/><div className="chart"><ResponsiveContainer width="100%" height={260}><AreaChart data={usageTrend}><XAxis dataKey="day"/><YAxis/><Tooltip/><Area dataKey="cost" name="Daily cost"/><Area dataKey="requests" name="Requests"/></AreaChart></ResponsiveContainer></div></Card></div>}
function Evaluations(){return <div className="grid page"><Card className="wide"><SectionTitle icon={ClipboardCheck} title="Evaluation Framework" subtitle="Compare models across task-specific eval suites before changing production traffic."/><div className="chart"><ResponsiveContainer width="100%" height={320}><BarChart data={evalRuns}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="suite"/><YAxis/><Tooltip/><Legend/><Bar dataKey="baseline" name="Current baseline"/><Bar dataKey="modelA" name="Model A"/><Bar dataKey="modelB" name="Model B"/></BarChart></ResponsiveContainer></div></Card><Card><SectionTitle icon={CheckCircle2} title="Promotion Gate" subtitle="Production readiness policy."/><div className="gate"><div><CheckCircle2/><span>Quality +4 pts vs baseline</span></div><div><CheckCircle2/><span>Policy score above 90</span></div><div><AlertTriangle/><span>Grounding failure review required</span></div></div><button className="primary full">Approve 10% canary</button></Card><Card className="wide"><SectionTitle icon={FileSearch} title="Eval Test Results" subtitle="Failure analysis makes model quality operational, measurable, and repeatable."/><div className="table eval"><div className="row header"><span>Test Case</span><span>Result</span><span>Score</span><span>Failure Mode</span></div>{evalTable.map(t=><div className="row" key={t.test}><span>{t.test}</span><span><Badge tone={t.result==="Pass"?"good":"bad"}>{t.result}</Badge></span><span>{t.score}</span><span>{t.failure}</span></div>)}</div></Card></div>}
function Routing(){return <div className="grid page"><Card className="wide"><SectionTitle icon={Route} title="AI Routing Engine" subtitle="Route requests by complexity, risk, latency sensitivity, quality requirements, and cost ceiling."/><div className="chart"><ResponsiveContainer width="100%" height={320}><LineChart data={routingImpact}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis/><Tooltip/><Legend/><Line dataKey="baseline" name="Baseline spend ($K)" strokeWidth={3}/><Line dataKey="optimized" name="Optimized spend ($K)" strokeWidth={3}/></LineChart></ResponsiveContainer></div></Card><Card><SectionTitle icon={Zap} title="Routing Decision" subtitle="Live example request."/><div className="routingBox"><Badge tone="warn">Medium risk</Badge><h4>Customer support refund edge case</h4><p>Policy-sensitive response with order context and customer frustration signal.</p><ChevronRight/><h3>Routed to: Frontier model</h3><p>Reason: high policy risk + nuanced reasoning required.</p></div></Card><Card className="wide"><SectionTitle icon={GitBranch} title="Routing Rules" subtitle="PM-friendly controls for balancing quality, latency, cost, and safety."/><div className="cardsList">{routingRules.map(r=><div className="rule" key={r.route}><div><h4>{r.route}</h4><p>{r.reason}</p></div><div><Badge tone="neutral">{r.model}</Badge><span>{r.traffic}% traffic</span><strong>{r.savings}</strong></div></div>)}</div></Card></div>}
function Governance(){return <div className="grid page"><Card><SectionTitle icon={ShieldCheck} title="Policy Health" subtitle="Risk score by governance domain."/><div className="chart small"><ResponsiveContainer width="100%" height={300}><RadarChart data={policyScores}><PolarGrid/><PolarAngleAxis dataKey="domain"/><PolarRadiusAxis angle={30} domain={[0,100]}/><RRadar dataKey="score"/><Tooltip/></RadarChart></ResponsiveContainer></div></Card><Card className="wide"><SectionTitle icon={AlertTriangle} title="Governance Events" subtitle="Monitor blocked, redacted, escalated, and audited AI activity."/><div className="cardsList">{governanceEvents.map(e=><div className="gov" key={e.id}><div><Badge tone={e.severity==="High"?"bad":e.severity==="Medium"?"warn":"neutral"}>{e.severity}</Badge><h4>{e.issue}</h4><p>{e.app} · {e.id}</p></div><strong>{e.action}</strong></div>)}</div></Card><Card className="wide"><SectionTitle icon={Scale} title="Governance Controls" subtitle="Designed for enterprise trust: prevent, detect, explain, and audit."/><div className="controlGrid">{["PII redaction","Prompt injection detection","Sensitive topic escalation","Source citation required","Role-based model access","Audit log retention","Human approval for high-risk actions","Eval gate before deployment"].map(x=><div className="control" key={x}><CheckCircle2 size={16}/><span>{x}</span></div>)}</div></Card></div>}
function Architecture(){return <div className="grid page"><Card className="wide"><SectionTitle icon={Network} title="Platform Architecture" subtitle="A unified control plane for prompt management, routing, evals, observability, governance, and model gateway operations."/><div className="arch">{architectureNodes.map((n,idx)=><motion.div className="archNode" key={n} whileHover={{scale:1.04}}><span>{idx+1}</span><h4>{n}</h4></motion.div>)}</div></Card><Card><SectionTitle icon={BrainCircuit} title="Request Flow" subtitle="From app request to safe model response."/><ol className="flow"><li>Classify request complexity and risk.</li><li>Assemble context using prompt registry and retrieval.</li><li>Apply governance policies and PII redaction.</li><li>Route to best model using eval and cost rules.</li><li>Log tokens, latency, output quality, and audit event.</li></ol></Card><Card className="wide"><SectionTitle icon={Radar} title="What This Proves as a PM Artifact" subtitle="This is the product judgment behind the prototype."/><div className="pmGrid"><div><h4>Business value</h4><p>Controls runaway AI spend while improving quality and latency.</p></div><div><h4>Technical fluency</h4><p>Shows understanding of prompt caching, model routing, evals, gateways, and observability.</p></div><div><h4>Enterprise readiness</h4><p>Includes governance, audit trails, human review, and policy controls.</p></div><div><h4>Product strategy</h4><p>Turns AI from scattered experiments into managed infrastructure.</p></div></div></Card></div>}
function App(){const[active,setActive]=useState("overview");const ActiveView={overview:Overview,prompts:PromptAnalytics,evals:Evaluations,routing:Routing,governance:Governance,architecture:Architecture}[active];return <main><aside><div className="logo"><div><Sparkles size={22}/></div><span>SignalForge</span></div><nav>{views.map(v=>{const Icon=v.icon;return <button key={v.id} className={active===v.id?"active":""} onClick={()=>setActive(v.id)}><Icon size={18}/><span>{v.label}</span></button>})}</nav><div className="sideNote"><Badge tone="good">Live prototype</Badge><p>Clickable LLMOps control plane for AI PM portfolio storytelling.</p></div></aside><section className="content"><header><div><h1>{views.find(v=>v.id===active).label}</h1><p>Manage AI quality, cost, latency, routing, evaluation, and governance from one enterprise platform.</p></div><button className="primary">Create rollout plan <ChevronRight size={16}/></button></header><ActiveView/></section></main>}
createRoot(document.getElementById("root")).render(<App/>);
